# lucky_fortune
# 🚀 Flask Web App

## 📦 Installation

Install the required packages:

```bash
pip install flask
```

---

## ▶️ Running the Application

Run the Flask app **in the same directory as `app.py`**:

```bash
flask --app app run --debug
```

Debug mode enables auto-reload and detailed error messages during development.

---
